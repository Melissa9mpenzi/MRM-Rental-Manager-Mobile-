import 'package:flutter_test/flutter_test.dart';

/// Smoke test — full app widget test needs Firebase/router bootstrap (run on device).
void main() {
  test('CI smoke', () {
    expect(true, isTrue);
  });
}
